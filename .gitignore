# Local publish logs Claude writes after shipping
posts/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Secrets
# Note: .env IS tracked here with a placeholder. After cloning, run
# `git update-index --skip-worktree .env` so your local key edits never
# get staged. See the header comment in .env.
.env.local
.env.keys
*.pem
*.key

# Workspace folders the user might create for staging media
media/
drafts/
